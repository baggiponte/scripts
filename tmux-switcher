#! /usr/bin/env zsh

tmux-switcher () {
    local session="$(tmux list-sessions -F "#{session_name}"| fzf --ansi --height=40%)"

    if [[ ! -z "$TMUX" ]]; then
        tmux switch-client -t "$session"
    fi

    tmux attach -t "$session"
}

tmux-switcher
